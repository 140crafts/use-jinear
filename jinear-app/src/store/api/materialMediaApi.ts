import { api } from "./api";

export const materialMediaApi = api.injectEndpoints({
  endpoints: (build) => ({
    //

    //
  })
});

export const {} = materialMediaApi;

export const {
  endpoints: {}
} = materialMediaApi;
